#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: integers to add
 * @b: second integer
 *
 * Return: returns the result
 */
int add(int a, int b)
{
	int addition = a + b;

	return (addition);
}
